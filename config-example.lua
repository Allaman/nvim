-- see ./lua/config/defaults for all
-- possible values
return {
  options = {
    relativenumber = true,
  },
  plugins = {
    alpha = {
      disable_dashboard_header = true,
    },
    spectre = {
      enable = false,
    },
  },
}
