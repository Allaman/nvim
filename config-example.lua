-- TODO: major refactoring is ongoing ...
-- see ./lua/config/defaults for all
-- possible values
return {
  options = {
    relativenumber = true,
  },
  plugins = {
    spectre = {
      enable = false,
    },
  },
}
